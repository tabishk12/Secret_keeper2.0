<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details Form</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .user-details-form {
            display: flex;
            flex-direction: column;
        }
        .user-details-form h2 {
            margin-bottom: 20px;
        }
        .user-details-form label {
            margin-bottom: 5px;
            font-weight: bold;
        }
        .user-details-form input,
        .user-details-form textarea,
        .user-details-form select {
            margin-bottom: 15px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            width: 100%;
            box-sizing: border-box;
        }
        .user-details-form button {
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            background-color: #5cb85c;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .user-details-form button:hover {
            background-color: #4cae4c;
        }
    </style>
</head>
<body>
    <div class="container">
        <form class="user-details-form" action="/details" method="POST">
            <h2>User Details</h2>
            <label for="fullName">Full Name</label>
            <input type="text" id="fullName" name="fullName" value="<%= user ? user.fullName : '' %>" required>
            <label for="username">Username</label>
            <input type="text" id="username" name="username" value="<%= user ? user.name : '' %>" required>
            
            <label for="password">Password</label>
            <input type="password" id="password" name="password" required>
            
            <label for="bio">Bio</label>
            <textarea id="bio" name="bio" rows="4"><%= user ? user.bio : '' %></textarea>
            
            <label for="profile-pic">Profile Picture</label>
            <input type="file" id="profile-pic" name="profilePic">
            
            <label for="dob">Date of Birth</label>
            <input type="date" id="dob" name="dob" value="<%= user && user.dob ? user.dob.toISOString().split('T')[0] : '' %>" required>  
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>
